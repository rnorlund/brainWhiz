server {
  listen 8080;
  root /usr/share/nginx/html;
  index index.html;
  # long cache for the big static bundles; short for html
  location ~* \.(js|glb|png)$ { add_header Cache-Control "public, max-age=86400"; }
  location / { try_files $uri $uri/ /index.html; }
}
